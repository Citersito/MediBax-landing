---
export interface Props {
    icon: string;
    title: string;
    description: string;
}
const { icon, title, description } = Astro.props;
---

<div class="card">
  <div class="icon">{icon}</div>
  <h3>{title}</h3>
  <p>{description}</p>
</div>
